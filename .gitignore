# mega_agent runtime state — never commit
models.json
models.json.enc
.master-key.salt

# state dirs
.tasks/
.worktrees/
.team/
.runtime-tasks/
.cron/

# memory & hooks (may contain user-specific notes)
CLAUDE.md
.hooks.json
.memory/

# python
__pycache__/
*.pyc

# Demo signed-skill catalogue ships as its own git repo at ./mega-skills/
# so you can split it off (`cd mega-skills && git remote add origin ... && git push`).
# Keep it untracked here to avoid submodule-pointer noise.
mega-skills/
